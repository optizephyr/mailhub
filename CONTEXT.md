# 邮件处理中心

本地把邮箱里筛出的重要邮件，经渠道提醒用户。

## Language

**dry-run**:
一次不向渠道写入、不推进收取游标的运行。启用中的渠道仍要在开头做配置校验；校验失败则此次运行失败，与是否 dry-run 无关。dry-run 不向 Bark 自建服务器发请求。
_Avoid_: 测试模式、演练、只检查配置（把预览日历/提醒事项排除在外）

**渠道**:
Resolve 之后、面向用户的落点。当前三条：Apple 日历、Apple 提醒事项、Bark。
_Avoid_: 后端、sink、通知渠道

**Apple 日历**:
有明确起止的日程所在的渠道。
_Avoid_: 日历后端、Apple Calendar（对用户说话时）

**Apple 提醒事项**:
开放时间窗口所在的渠道；不是即时推送。
_Avoid_: Reminder、待办

**未启用**:
渠道已接入 Dispatch，但本轮不向用户送达。Bark 开关关闭则为未启用；缺密钥或自建地址只要开关是关的，不构成失败。
_Avoid_: 关闭、禁用插件、skip

**Bark**:
即时推送渠道。只把「现在值得知道」送到用户，不持有可改可取消的日程或提醒条目。一个渠道对应一个送达密钥（一台设备），送到用户配置的自建服务器，不使用官方默认地址。启用由显式开关控制，默认关。开关打开但密钥或服务器地址缺失时，整次运行失败。接入 Dispatch，但在「哪些邮件进入该渠道」定下来之前，不产生动作。
_Avoid_: Bark 通知（当作另一条流水线）、推送服务、官方推送地址
