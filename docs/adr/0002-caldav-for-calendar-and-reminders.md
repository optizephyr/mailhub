# 日历与提醒事项写入配置的 CalDAV 落点

处理中心要在无头环境定时跑，本机「日历 / 提醒事项」App 当写入面做不到。iCloud CalDAV 仍能写日程，但主账户的提醒事项已不在该协议上，写入 VTODO 也不会出现在现在的提醒事项 App 里。因此两条渠道都写同一套第三方 CalDAV 账户：一本日历、一个列表；不经 AppleScript，也不直连 iCloud。

渠道名、送达与未启用见 `CONTEXT.md`。
